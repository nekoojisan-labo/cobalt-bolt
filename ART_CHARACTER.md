# ART_CHARACTER.md — 主人公アニメーションの統一設計（再設計／単一系統）

> 経緯: リグ(パーツ分割)と塗り一枚絵を**混在**させた結果、状態が切り替わると脚や姿勢が食い違った
> （例: 走り→走り撃ちで脚が"歩き"に変わる、待機→チャージで姿勢が変わる）。
> **対策: 混在をやめ、主人公は「塗りフルフレーム一系統」に統一**。下記の単一ルールで全状態を作る。
> （リグは主人公では不使用。ボスのみリグ継続。）

## ★ 単一ルール（これが設計の核）
1. **マスターは1体**: 既存 `player_idle.png` / `player_run.png` のコバルト青ロボ＝正準デザイン。全コマでプロポーション・パレット・シルエットを完全統一。
2. **運動ベース**＝脚・胴の動き: `idle`(待機) / `run`(前傾ダッシュ) / `jump`。これらは“素の動き”で、**腕は構え（バスター下げ/走りの自然位置）**。
3. **武器バリアント**＝ベースの各コマに**腕(バスター)だけ差し替えて合成**したもの。**脚・胴・接地・コマ数・各コマの位置はベースと完全一致**。違うのは前side のバスター腕だけ：
   - `shoot`   = `idle` の各コマ + バスター前方水平
   - `runshoot`= `run`  の各コマ + バスター前方水平（**脚は run と1コマもズレない**）
   - `jumpshoot`= `jump` の各コマ + バスター前方水平
   - `charge`  = `idle` の各コマ + バスター前方に蓄光オーブ（**姿勢は idle のまま**変えない）
4. これにより「走り↔走り撃ち」「待機↔チャージ」で**脚・姿勢が一致**し、切替で歩きに変わったり姿勢が飛んだりしない。動くのは腕だけ。

## 生成方法（重要・既存ベースから“派生”させる）
- **新規に描き起こさない**。`player_run.png`(8コマ) / `player_idle.png`(5コマ) を**そのまま土台**にし、
  各コマの前side バスター腕の領域だけを、目的の腕に**差し替え/上書き合成**する（脚・胴は元画素を保持）。
- 腕の差し替えは肩の付け根を基準に。砲口の高さ・向きはコマ間で一貫。

## 作るファイル（assets/）
| ファイル | 土台 | コマ | 腕 |
|---|---|---|---|
| `player_runshoot.png` | `player_run.png` | 8 | **前方水平に伸ばしたバスター**（マズルは砲口前端、走り撃ちで保持） |
| `player_charge.png`（作り直し） | `player_idle.png` | 4〜5 | **前方にバスター＋シアン蓄光オーブが脈動**（姿勢はidleのまま） |
| `player_jumpshoot.png`（任意） | `player_jump.png` | 2 | 前方水平バスター |

- すべて元の `player_*` と同一寸法・同一フレーム数・透過PNG・同一スケール/接地。
- 仕上げに各コマの脚・胴が土台と一致しているか（走り↔走り撃ちで脚がズレないか）を確認。

## ゲーム側の対応（Claude）
- 主人公は `drawPlayer` で**全状態 `drawSheet('player', …)`（塗り）**を使用（リグ呼び出し撤去済み）。
- `runshoot` は run と脚一致、`charge` は idle と姿勢一致になるので、`fireHold` で走り撃ち時に腕を保持しても自然。
- jumpshoot を作ったら `ASSETS.player.anims.jumpshoot` を追加し、ジャンプ中の射撃で使用。
