# ART_PARTS.md — 旧・共通キャンバス型パーツ仕様

> 主人公の現行正典は `ART_CHARACTER.md` の関節リグ（`skel_*.png`）へ移行済み。
> この文書の `rig_player_*` は互換・履歴用、ボス用 `rig_boss_*` は引き続き有効。

> 目的：主人公とボスを**1枚絵フレームではなくパーツ分割（カットアウト・リグ）**にする。
> ゲーム側(`index.html`の`RIG`)は **脚→胴+頭→腕+武器** の順で**同一矩形**に重ねて合成し、
> 各パーツを独立に差し替え/アニメする。これにより「**走り撃ち＝脚はrun・腕はfireで伸ばしっぱなし**」
> 「**立ち撃ち＝動くのは腕だけ**」を実現する。画風は `ART_DESIGN.md`（高精細塗り2D・フルHD）に従う。

## ★ 最重要：共通キャンバス方式（位置合わせの要）
- **まず1体のマスターキャラを設計**し、その上で**各パーツだけを残して他を透過**にして書き出す。
- すべてのパーツは**同じキャンバス寸法・同じスケール・同じ立ち位置**で出力する
  （例：主人公は全パーツ 256×256 のキャンバス内で、そのキャラの定位置にパーツを置き、他は透過）。
- こうすれば別々に生成しても**重ねるだけで肩・腰・接地が自動で揃う**。
- **腕（バスター/キャノン）の肩関節の座標は down/fire/charge で完全一致**させること（ここがズレると差し替え時に飛ぶ）。
- 透過必須（チャイマキー→PNGアルファ）。影や地面は描かない。キャラの向きは**右向き**（ゲーム側が左を反転）。

---

## ① 主人公 COBALT BOLT（キャンバス 256×256 / 透過 / 横ストリップ）
パレット：コバルト/アズール青＋シアン発光＋白トリム、肌は明るいベージュ。`ART_DESIGN.md`準拠。

| ファイル | 中身（**そのパーツのみ**・他は透過） | コマ | 出力サイズ |
|---|---|---|---|
| `rig_player_legs.png` | **脚＋腰のみ**。[0]=直立(idle) / [1..4]=走りサイクル / [5]=ジャンプ(脚を畳む) | 6 | 1536×256 |
| `rig_player_body.png` | **胴＋頭＋ヘルメット＋奥側の腕**（バスターでない方）。脚と前腕の場所は透過 | 1 | 256×256 |
| `rig_player_arm_down.png` | **前side のバスター腕のみ**・体側に下ろした待機姿勢 | 1 | 256×256 |
| `rig_player_arm_fire.png` | **前side のバスター腕のみ**・**真っ直ぐ前方へ水平に伸ばした射撃姿勢**。砲口は前端 | 1 | 256×256 |
| `rig_player_arm_charge.png` | **前side のバスター腕のみ**・前方へ伸ばしつつ砲口にシアンの蓄光オーブ（2コマ点滅） | 2 | 512×256 |

- 脚6コマは**腰の高さ・接地ラインを全コマ同一**に（胴と継ぎ目が合うように）。
- 腕3種は**肩の付け根を同一座標**に。fireは砲口がキャラ前方端（バレットの射出点付近）に来るよう水平に。

## ② ボス（キャンバス 512×512 / 透過 / 横ストリップ）
パレット：深紅＆鋼鉄＋黄発光コア。`ART_DESIGN.md`準拠。**専用武器＝大型アームキャノン**。

| ファイル | 中身（**そのパーツのみ**・他は透過） | コマ | 出力サイズ |
|---|---|---|---|
| `rig_boss_body.png` | **本体（胴+頭+脚+胸コア）のみ**・浮遊/呼吸の2コマ。キャノン腕の場所は透過 | 2 | 1024×512 |
| `rig_boss_arm_idle.png` | **アームキャノンのみ**・構え | 1 | 512×512 |
| `rig_boss_arm_charge.png` | **アームキャノンのみ**・砲口に溜めの発光（2コマ） | 2 | 1024×512 |
| `rig_boss_arm_fire.png` | **アームキャノンのみ**・発射（マズルフラッシュ）2コマ | 2 | 1024×512 |

- キャノン腕の**肩関節を idle/charge/fire で同一座標**に。本体側のキャノン接続部は透過にして腕で隠す。

---

## 生成手順（推奨・1個ずつコミット）
1. まず **`rig_player_legs.png` と `rig_player_body.png` と `rig_player_arm_fire.png`** の3枚を作って
   `assets/` に置き、`[codex] rig: player core 3 parts` で**コミット**（部分進捗を保全）。
2. 残りの主人公腕（down/charge）→ コミット。
3. ボスの body → arm_idle/charge/fire → 各コミット。
4. 各生成後、可能なら自分で透過と寸法を確認。**`HANDOFF.md` 先頭に「置いた素材一覧」を追記**して Claude に渡す。

Claude 側は `index.html` の `RIG` フックに自動で載るので（パーツが揃えば自動優先）、
`node tools/preview.js`→PNG目視で**腕だけ伸ばしっぱなし／継ぎ目**を確認し、必要なら各パーツの `ox/oy` で微調整して反映・push する。
